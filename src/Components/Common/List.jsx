import React from "react";

const List = (props) => {
  //   console.log(props);
  return (
    <h1>jeeli</h1>
    // <ul>
    //   {tasks.map((item) => {
    //     return <li>{item}</li>;
    //   })}
    // </ul>
  );
};

export default List;
